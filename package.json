{
  "name": "fe-game-view-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for game view frontend projects",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter mengdao-game-view dev",
    "build:md:pro": "pnpm --filter mengdao-game-view build:analyze",
    "preview:md": "pnpm --filter mengdao-game-view preview:dist",
    "lint": "pnpm --filter mengdao-game-view lint",
    "type-check": "pnpm --filter mengdao-game-view type-check"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
